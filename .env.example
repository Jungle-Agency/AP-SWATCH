# Stripe — Dashboard Stripe → Developers → API keys
# Test mode (sk_test_...) or live mode (sk_live_...)
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxx

# Optionnel — Gemini (AI Studio scaffold, non utilisé en prod)
# GEMINI_API_KEY=
